// import { httpPost, httpGet } from '../utils/request';

// export interface Resp {
//     status: number;
//     msg: string;
//     result?: any;
// }

// export const uploadToIPFS = (body: any) => httpPost('/upload', body)
// export const download = (cid: string) => httpGet(`/ipfs/${cid}`, {})

// export const postLogin = (params = {}) => httpPost('/api/user/login', params);

// export const getTaskList = (params = {}) => httpGet('/api/task/list', params);

// export const createTask = (param = {}) => httpPost('/api/task/create', param);
